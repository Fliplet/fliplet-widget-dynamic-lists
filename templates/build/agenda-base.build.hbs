<div class="new-agenda-list-container loading \{{#ifCond social '&&' social.bookmark}}has-agenda-toggle\{{/ifCond}}">
  \{{#ifCond social '&&' social.bookmark}}
  <div class="agenda-list-controls">
    <div class="toggle-agenda" data-mixitup-control="\{{id}}" data-toggle=".bookmarked">
      <span class="fa fa-bookmark-o"></span>
      <span class="fa fa-bookmark"></span>
      <div class="agenda-list-label">My Agenda</div>
    </div>
  </div>
  \{{/ifCond}}
  <!-- Days slider -->
  <div class="agenda-date-selector">
    <ul>
      <!-- Day filter will be added here -->
    </ul>
  </div>

  <!-- Agenda Cards -->
  <div class="agenda-cards-wrapper" id="agenda-cards-wrapper-\{{id}}">
    <div class="agenda-list-holder">
      <!-- Cards will be rendered here -->
    </div>
    <!-- States messages -->
    <div class="agenda-state-messages">
      <div class="loading-holder">
        <i class="fa fa-circle-o-notch fa-spin"></i> Loading agenda...
      </div>
    </div>
  </div>
</div>

<!-- Detail view overlay -->
<div class="agenda-detail-overlay" id="agenda-detail-overlay-\{{id}}">
  <div class="agenda-detail-overlay-screen"></div>
  <div class="agenda-detail-overlay-wrapper">
    <div class="agenda-detail-overlay-panel">
      <div class="agenda-detail-overlay-close \{{#if previousScreen}}go-previous-screen\{{/if}}">
        <i class="fa fa-times-thin fa-2x"></i>
      </div>
      <div class="agenda-detail-overlay-content">
        <div class="agenda-detail-overlay-content-holder">
          <!-- Details will be added here -->
        </div>
      </div>
    </div>
    <div class="agenda-detail-overlay-close tablet \{{#if previousScreen}}go-previous-screen\{{/if}}">
      <i class="fa fa-times-thin fa-2x"></i>
    </div>
  </div>
</div>

\{{#if showAddEntry}}
<div class="dynamic-list-add-item">
  <i class="fa fa-plus"></i>
</div>
\{{/if}}